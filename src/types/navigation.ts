export enum NavLink {
  services = '#services',
  portfolio = '#portfolio',
  contacts = '#contacts',
}

export interface INavigationDictionary {
  services: string;
  portfolio: string;
  contacts: string;
}
